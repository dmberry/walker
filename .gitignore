.DS_Store
._*

# Scratch. Source art packs live here and none of it ships: the Quaternius
# character, survival, gun and nature kits together run to about 670 MB, and the
# game loads a few megabytes of it. Only what is used is copied into assets/,
# and tools/import-nature.py is the record of which files and at what size.
_tmp/

# Local preview config, carries absolute paths from this machine.
.claude/
